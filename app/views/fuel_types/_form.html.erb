<%= form_for(@fuel_type) do |f| %>
  <% if @fuel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fuel_type.errors.count, "error") %> prohibited this fuel_type from being saved:</h2>

      <ul>
      <% @fuel_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form">
    <table>
    <tr>
      <td><%= f.label :name %></td>
      <td><%= f.text_field :name %></td>
    </tr>
    </table>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
