<%= form_for(@repair_type) do |f| %>
  <% if @repair_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repair_type.errors.count, "error") %> prohibited this repair_type from being saved:</h2>

      <ul>
      <% @repair_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="form">
    <tr>
      <td><%= f.label :name %></td>
      <td><%= f.text_field :name %></td>
    </tr>
  </table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
